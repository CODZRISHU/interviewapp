services:
  - type: web
    name: kevin-ai-backend-v2
    env: python
    rootDir: backend
    branch: release/v2-beta
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: false
